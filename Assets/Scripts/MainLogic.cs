using UnityEngine;
using System.Collections;

public class MainLogic : MonoBehaviour {

    /**
     * Programbeskrivelse
     * 
     * Requirements:
     * 2 Elevatorer
     * 3 etager med knapper
     * Knapper i elevatorerne til at vælge etage og en nødstop knap
     * 
     * 
     * Ved tryk på knappen på en etage vil den nærmest elevator der ikke er i gang med noget, bevæge sig til den pågældende etage og åbne dørene
     * 
     * 
     * */

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

	}
}
